# 更新记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] — 2026-08-20

### 新增

- **注册**：可以直接在 app 里注册账号，不用先去网站
- **Passkey 登录走系统原生弹窗**，不再打开浏览器
- **图标跟随主题**：切到紫色主题，Dock 里的图标也变紫
- **生成记录可以删除**，失败的和成功的都行；删除时可选「只删记录」或「连图一起删」
- **退出登录会请服务器作废令牌**，不只是本地清掉
- **空间流水分页**，一页 5 条，翻页即时切换不发请求

### 变更

- **品牌色**：绿 `#5DE31D` → `#90FF3A`，紫 `#8E47FF` → `#7624F4`。两套色阶都重算，
  不是只换一格
- **侧边栏**换成一根会滑动的发光条，停在当前页那一行旁边
- **概览页与设置页重排**。原来是写死的两列，概览页左下空着 523pt，窗口拉宽后
  左右各留白 236pt。改为按可用宽度求解，最宽三列；默认窗口下列宽与以前完全一致
- **签到的「一天」按本地时区算**，不再是 UTC。原来东八区的日界落在早上 8 点
- **Google / GitHub 登录复用 Safari 登录态**，不再每次重输密码
- **昵称点一下才进编辑**，旁边出「保存」。原来是常驻输入框、失焦即存
- **修改密码 / 添加 Passkey 不再自动发验证码**，发不发由用户按
- **AI 水印只用 `gpt-image-2`**，透明底改本机抠，不再依赖上游
- 最近上传的缩略图改成 4:3 并在悬浮时显示文件名；上传趋势的柱形图撑满卡片高度

### 移除

- **登录页的「访问令牌」入口**。账号密码与三个 OAuth 已覆盖所有情况，包括自建服务器。
  令牌本身没变，它仍是登录后存进钥匙串的东西，只是不再让人手输
- `PasskeySheet`（52 行死代码，触发它的枚举值全项目零赋值）
- 11 条无人引用的文案与一个够不着的字重

### 修复

- app 每次启动都要重新输密码
- 上传的原图带着 GPS 直接上公网 —— EXIF 剥离一直没接上调用点
- 点解绑 pic.bi 报「令牌无效」，其实令牌是好的
- 绑了 R2 的账号在设置页显示成字面的 `user_r2`
- 概览页按 ⌘R 刷新，页面上最大的那个数字纹丝不动
- 空间流水的金额显示成 `−−104 KB`（两个减号）
- 生成记录里鼠标停在「复制链接」上，浮出来的却是提示词和模型名
- 编辑器 19 处审查问题

## [0.2.0] — 2026-08-19

首个 **Developer ID 签名并公证** 的版本 —— 下载解压即可打开，不需要右键，
也不用去系统设置里放行。

### 新增

- **AI 生成图片**：写一句描述出图，结果自动进图库，和手动上传的图完全一样
  （去重、外链、短链、备份都有）
- **AI 修图**：从图库挑 1–4 张或直接拖本地文件，按提示词改图。内置去水印、
  去除路人杂物、换背景、修复老照片、黑白上色、人像精修等 11 个一键预设
- **pic.bi 关联**：关联后 AI 生成可用 4K 清晰度
- **编辑独立成页** + 本机智能修图
- **自建实例登录**：登录页可填自己部署的服务器地址
- **App 内配置存储位置（BYOS）**：R2 / S3 / B2 / Spaces / OSS / COS

### 修复

- 打包时 `Info.plist` 的版本号没被展开
- 受限授权缺少描述文件导致 AMFI 拒绝加载 —— 表现是 `spctl` 全绿但谁都打不开。
  发布流程因此加了一道带隔离属性的真实启动自检

## [0.1.0]

首个版本。上传、图库、配额签到、上传前编辑（裁剪 / 马赛克 / 旋转 / 水印）、
监控目录自动上传、整库导出。纯 Swift 6 + SwiftUI，零第三方依赖。

[0.3.0]: https://github.com/gentpan/openimg-app/releases/tag/v0.3.0
[0.2.0]: https://github.com/gentpan/openimg-app/releases/tag/v0.2.0
[0.1.0]: https://github.com/gentpan/openimg-app/releases
